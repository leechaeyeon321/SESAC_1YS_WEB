<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <h3>post로 정보 받기</h3>
    <form action="/postForm" method="post">
        이름<input type="text" name="id" required>
        <fieldset>
            <legend>성별</legend>
            <input type="radio" name="gender" value="남" id="men" required> <label for="men">남자</label>
            <input type="radio" name="gender" value="여" id="women" required> <label for="women">여자</label>
        </fieldset>
        <br>
        <fieldset>
            <legend>생년월일</legend>
            <select name="birthYear">
                <%for(var i = 1940; i<=2022; i++) {%>
                <option value="<%=i%>"><%=i%>년</option>
                <% } %>
            </select>
            <select name="birthMonth">
                <%for(var i = 1; i<=12; i++) {%>
                <option value="<%=i%>"><%=i%>월</option>
                <% } %>
            </select>
            <select name="birthDay">
                <%for(var i = 1; i<=31; i++) {%>
                <option value="<%=i%>"><%=i%>일</option>
                <% } %>
            </select>
        </fieldset>
        <br>
        <fieldset>
            <legend>관심사</legend>
            <input type="checkbox" name="interest" value="travel" id="travel"/> <label for="travel">여행</label>
            <input type="checkbox" name="interest" value="fas" id="fas"/> <label for="fas">패션</label>
            <input type="checkbox" name="interest" value="fd" id="fd"/><label for="fd">음식</label>
        </fieldset>
        <br>
        <fieldset>
            <legend>선택</legend>
            좋아하는 색상<input type="color" id="color" name= "color" value="#ff0000"/>
            <br>
            좋아하는 숫자<input type="number" id="num" name= "num" value=""/>
        </fieldset>
        <br>
        <button type="submit">회원가입</button>
    </form>
    <script>
        console.log(document.forms["getform"])
    </script>
</body>
</html>
