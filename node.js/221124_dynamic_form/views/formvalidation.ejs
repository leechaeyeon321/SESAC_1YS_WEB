<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <h3>아이디와 비밀번호 만들기</h3>
    <form action="/postForm" method="post">
        <input
            type="text"
            name="id"
            placeholder="ID"
            pattern="^([a-zA-Z0-9]){6,}$"
            title="대소문자, 숫자, 6글자 이상"
            required
        />
        <input
            type="password"
            name="pw"
            placeholder="PW"
            pattern="^([a-z0-9 !^*~]){8,12}$"
            title="소문자, 숫자로 8자리 이상 12자리 이하, 특수문자는 !^*~만 가능"
            required
        />
        <input
            type="text"
            name="phone number"
            placeholder="Phone Number"
            pattern="^(010|011|016|017|018|019)-\d{3,4}-\d{4}$"
            title="올바른 번호를 입력하세요"
            required
            />
        <button type="submit">submit</button>
    </form>
    <script>
        //name지정하면 script에서 불러올 수 있음. form태그의 name은 선택 사항.
        console.log(document.forms["getform"])
    </script>
</body>
</html>
